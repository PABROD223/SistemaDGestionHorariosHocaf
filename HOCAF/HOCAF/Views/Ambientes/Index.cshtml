@model IEnumerable<HOCAF.Models.Ambiente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sede.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoAmbiente.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Sede.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoAmbiente.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idAmbiente }) |
            @Html.ActionLink("Details", "Details", new { id=item.idAmbiente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idAmbiente })
        </td>
    </tr>
}

</table>
