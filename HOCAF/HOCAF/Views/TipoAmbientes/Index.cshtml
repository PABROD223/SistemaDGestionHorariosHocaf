@model IEnumerable<HOCAF.Models.TipoAmbiente>

@{
    ViewBag.Title = "TipoAmbiente";
}

<h2>Tipo Ambientes</h2>

<p>
    @Html.ActionLink("Crear nuevo tipo ambiente", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            <a href="@Url.Action("Edit", new { id=item.idTipoAmbiente })"><span class="glyphicon glyphicon-edit"></span></a>
        </td>
        <td>
            <a href="@Url.Action("Details", new { id=item.idTipoAmbiente })" class="text-success"><span class="glyphicon glyphicon-eye-open"></span></a>
        </td>
        <td>
            <a href="@Url.Action("Delete", new { id=item.idTipoAmbiente })" class="text-danger"><span class="glyphicon glyphicon-remove"></span></a>
        </td>
    </tr>
}

</table>
