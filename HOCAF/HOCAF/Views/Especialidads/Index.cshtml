@model IEnumerable<HOCAF.Models.Especialidad>

@{
    ViewBag.Title = "Index";
}

<h2>Especialidad</h2>

<p>
    @Html.ActionLink("Crear nueva especialidad", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Editar especialidad", "Edit", new { id=item.idEspecialidad }) |
            @Html.ActionLink("Detalles especialidad", "Details", new { id=item.idEspecialidad }) |
            @Html.ActionLink("Eliminar especialidad", "Delete", new { id=item.idEspecialidad })
        </td>
    </tr>
}

</table>
