@model IEnumerable<HOCAF.Models.Trimestre>

@{
    ViewBag.Title = "Index";
}

<h2>Registrar Trimestres</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idTrimestre }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.idTrimestre }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.idTrimestre })
        </td>
    </tr>
}

</table>
